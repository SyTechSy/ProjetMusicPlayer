import 'package:flutter/material.dart';

class PageChansonsVente extends StatelessWidget {
  const PageChansonsVente({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(
        children: [
          Text("data"),
          Text("data 2"),
        ],
      ),
    );
  }
}


